This folder is intentionally kept empty in the public repository.
All original Apple assets used for local development or deployment
are stored in `public/local-assets/`, which is intentionally excluded
from version control.